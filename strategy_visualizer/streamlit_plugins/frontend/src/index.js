import React from "react";
import ReactDOM from "react-dom";
import PlotlyZoomSync from "./PlotlyZoomSync";

// 使用兼容的 ReactDOM.render API
ReactDOM.render(
  <PlotlyZoomSync />,
  document.getElementById("root")
);
